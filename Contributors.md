# Contributors

The full **ranking and per-repository breakdown** now lives in `**[ProductionReport.md](ProductionReport.md#contributors)`** together with production metrics.

Regenerate everything from the `.github` folder (`npm install` once, then):

```bash
node scripts/generate-production-report.mjs --org HyperlinksSpace
```

See `[Update.md](Update.md)` for setup.